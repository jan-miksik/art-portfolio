/**
 * Timing constants for delays and timeouts
 */

/**
 * Delay after mouse up before resetting drag state
 * Prevents accidental clicks after dragging
 */
export const DRAG_RESET_DELAY_MS = 500

/**
 * Delay between publishing pieces to Contentful
 * Prevents rate limiting and allows for proper sequencing
 */
export const PUBLISH_DELAY_MS = 200

