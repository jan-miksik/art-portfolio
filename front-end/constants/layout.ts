/**
 * Layout and positioning constants
 * These values control the canvas positioning, offsets, and edge calculations
 */

/**
 * Base offset for positioning pieces on the canvas
 * Used to center the canvas and provide padding for drag operations
 */
export const LEFT_OFFSET = 5000
export const TOP_OFFSET = 5000

/**
 * Edge position padding for desktop view
 * Additional space added when calculating canvas boundaries for desktop
 */
export const DESKTOP_EDGE_PADDING = 9000

/**
 * Edge position padding for mobile view
 * Additional space added when calculating canvas boundaries for mobile
 */
export const MOBILE_EDGE_PADDING = 500

/**
 * Default initial position offsets for new pieces
 * Used when dropping new images on the canvas
 */
export const NEW_PIECE_X_OFFSET = 6000
export const NEW_PIECE_Y_OFFSET = 4000

/**
 * Default web display sizes for new pieces (in pixels)
 */
export const DEFAULT_WEB_WIDTH = 350
export const DEFAULT_WEB_WIDTH_MOBILE = 250

